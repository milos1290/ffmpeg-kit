# FFmpegKit Platform Interface for Flutter

A common platform interface for the [`ffmpeg_kit`][1] plugin.

This interface allows platform-specific implementations of the `ffmpeg_kit`
plugin, as well as the plugin itself, to ensure they are supporting the
same interface.

[1]: ../ffmpeg_kit
